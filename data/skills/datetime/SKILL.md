# datetime

Get the current local date, time, weekday, and timezone. Reads the system clock directly — no external API needed.

## When to use

- User asks what time it is, what day it is, today's date
- Any task that needs the current time (scheduling, logging, calendar)
- User asks for the time in a different timezone
